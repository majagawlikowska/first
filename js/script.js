console.log("Hi there, (❁´◡`❁)");

{
const TaggleEmail = () => {
   footer__header.innerText ("e-mail") === button.innerText ("Pokaż") = footer__header.innerText ("maja@poczta.pl") === button.innerText ("Ukryj")
   footer__header.innerText ("maja@poczta.pl") === button.innerText ("Ukryj") = footer__header.innerText ("e-mail") === button.innerText ("Pokaż")
}

const init = () => {
button = document.querySelector(".button");
button.addEventListener ("click" TaggleEmail);

}
}


//tak było: 
let button = document.querySelector(".button");
let footer__header = document.querySelector(".footer__header");

button.addEventListener("click", () => {
  if (footer__header.innerText === "e-mail") {
    footer__header.innerText = "maja@poczta.pl";
  } else {
    footer__header.innerText === "maja@poczta.pl";
    footer__header.innerText = "e-mail";
  }
  if (button.innerText === "Pokaż") {
    button.innerText = "Ukryj";
  } else {
    button.innerText === "Ukryj";
    button.innerText = "Pokaż";
  }
});
//



